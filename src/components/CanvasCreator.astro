---
import CanvasCreatorApp from './CanvasCreatorApp.astro';

interface Props {
  canvasId: string;
  locale?: string;
}

const { canvasId, locale = 'en' } = Astro.props as Props;
---

<CanvasCreatorApp canvasId={canvasId} locale={locale} />
