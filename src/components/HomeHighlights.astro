---
const highlights = [
  {
    title: 'Design Iteratively',
    description: 'Adopt a cycle-based approach for continuous improvement and planning.',
  },
  {
    title: 'Mitigate API Risks',
    description: 'Identify and manage key business and compliance issues early in the design.',
  },
  {
    title: 'Align Monetization',
    description: 'Ensure APIs deliver value with strategies tied to business opportunities.',
  },
  {
    title: 'Strengthen Governance',
    description: 'Establish clear roles, standards, and metrics for consistent API management.',
  },
];
---

<section class="home-highlights">
  <h2>Your key <strong>itto</strong> ami actor begins</h2>
  <div class="highlight-grid">
    {highlights.map((item) => (
      <article class="highlight-card">
        <h3>{item.title}</h3>
        <p>{item.description}</p>
      </article>
    ))}
  </div>

  <div class="highlight-lines" aria-hidden="true">
    <span class="line-label active">API Product Strategy</span>
    <span class="line-label">API Lifecycle Core Stations</span>
    <span class="line-label">API Consumer Experience</span>
  </div>
</section>

<style>
  .home-highlights {
    margin-top: 3rem;
    border-top: 1px solid var(--sl-color-hairline);
    padding-top: 2rem;
  }
  .home-highlights h2 {
    text-align: center;
    font-weight: 400;
    font-size: clamp(1.8rem, 3vw, 2.8rem);
    margin-bottom: 1.4rem;
    color: #2a2238;
  }
  .home-highlights h2 strong {
    font-weight: 700;
  }
  .highlight-grid {
    display: grid;
    grid-template-columns: repeat(1, minmax(0, 1fr));
    gap: 1rem;
  }
  @media (min-width: 50rem) {
    .highlight-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
  @media (min-width: 72rem) {
    .highlight-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }
  .highlight-card {
    background: #fff;
    border: 1px solid #e7e2f3;
    border-radius: 0.5rem;
    padding: 1rem;
    box-shadow: 0 2px 10px rgba(30, 20, 64, 0.05);
  }
  .highlight-card h3 {
    margin: 0 0 0.5rem;
    color: #312744;
    font-size: 1.75rem;
    line-height: 1.1;
  }
  .highlight-card p {
    margin: 0;
    color: #5b5470;
    font-size: 0.95rem;
  }
  .highlight-lines {
    margin-top: 1.25rem;
    border-top: 3px solid #d7ccef;
    padding-top: 0.8rem;
    display: flex;
    flex-wrap: wrap;
    gap: 1rem 2rem;
    color: #5c5870;
    font-size: 1rem;
  }
  .line-label.active {
    color: #4e31a0;
    font-weight: 600;
  }
</style>
