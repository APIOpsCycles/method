---
title: GraphQL Canvas
description: Plan GraphQL schemas, query patterns, and resolvers.
canvasId: graphqlCanvas
---

import CanvasCreator from '../../components/CanvasCreator.astro';

const { canvasId } = frontmatter;

Use the GraphQL Canvas to outline types, relationships, and resolver responsibilities. It keeps your schema design aligned with client use cases and backend capabilities.

{canvasId ? (
  <CanvasCreator canvasId={canvasId} />
) : (
  <p>
    Open the canvas in Canvas Creator at{' '}
    <a href="https://canvascreator.apiopscycles.com" target="_blank" rel="noreferrer">canvascreator.apiopscycles.com</a>.
  </p>
)}
